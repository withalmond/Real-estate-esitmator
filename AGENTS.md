# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Real-estate-esitmator") is currently empty — it contains only a `README.md` with a title. There is no application code, no dependencies, no build system, and no services.

- **No package manager or dependency file** exists (`package.json`, `requirements.txt`, `Gemfile`, `go.mod`, etc.).
- **No dev server, test suite, linter, or build step** is configured.
- **No database or external service** dependencies are present.

Once application code is added, this section should be updated with:
- How to install dependencies and run the dev server.
- How to run lint, tests, and build.
- Any non-obvious environment caveats.
